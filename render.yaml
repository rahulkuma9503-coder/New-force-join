services:
  - type: web
    name: join-forces-bot
    env: docker
    plan: free
    branch: main
    dockerfilePath: Dockerfile
    envVars:
      - key: BOT_TOKEN
        sync: false  # set via Render dashboard manually
